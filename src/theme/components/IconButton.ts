import { StyleConfig } from '@chakra-ui/react';

export const IconButton: StyleConfig = {
  baseStyle: {
    height: 'unset',
    padding: '0.25em',
    color: 'inherit',
  },
  defaultProps: {
    colorScheme: 'none',
  },
};
